;; If in clojure
(ns if)

;; Main function

;; 
;;  

;;

(defn main 
  "Basic example

  You don't have to write else the second statement if else part

  You can have if statement without else part"
  []
  (if (== (mod 7 2) 0)
    "7 is even"
    "7 is odd")



  (if (== (mod 8 4) 0)
    "8 is divisible by 4")
  )


;; Returns

;; `7 is odd` 
;; `8 is divisible by 4`
